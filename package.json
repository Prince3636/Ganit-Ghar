{
  "name": "ganit-ghar",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Ganit Ghar – Offline Math Learning, Quiz & Mini-Games Suite for Android Mobile",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "generate-icons": "node scripts/generate-icons.js",
    "cap:sync": "npm run build && cap sync android",
    "cap:open": "cap open android",
    "build:apk": "npm run build && cap sync android && node scripts/build-apk-help.js"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.1",
    "@capacitor/cli": "^7.0.1",
    "@capacitor/core": "^7.0.1"
  },
  "devDependencies": {
    "vite": "^6.0.7"
  }
}
